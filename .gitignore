# Build artifacts and caches. Everything here is regenerated by the run steps
# in each lab's README; committing it would bury the source under noise and
# leak local build paths into the repository.
__pycache__/
*.py[cod]
.pytest_cache/
target/
build/
*.class
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
.venv/
venv/
