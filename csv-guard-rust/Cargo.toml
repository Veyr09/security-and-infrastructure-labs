[package]
name = "csvguard"
version = "0.1.0"
edition = "2021"

# No dependencies on purpose. A client can build this on a bare box with nothing
# but a Rust toolchain, and there is no supply chain to audit.
[dependencies]

[profile.release]
opt-level = 3
lto = true
